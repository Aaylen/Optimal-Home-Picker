export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <title>Optimal Home Picker</title>
      </head>
      <body>
        {children}
      </body>
    </html>
  );
}
